image: rikorose/gcc-cmake # TODO Change to our container

stages:
    - build
    - test
    - lint

cache:
    paths:
        - server/build
        - desktop/build

################
# Build Stages #
################

build-server:
    stage: build
    script:
        - cd server && ./build.sh
        # - lint.sh &&./lint.sh tidy TODO add docker image

build-desktop:
    stage: build
    script:
        - cd desktop && gradle build

###############
# Test Stages #
###############

test-server:
    stage: test
    script:
        - cd server && echo "TODO"

test-desktop:
    stage: test
    script:
        - cd desktop && gradle test

###############
# Lint Stages #
###############

lint-server:
    stage: lint
    script:
        - cd server && ./lint.sh format -8
