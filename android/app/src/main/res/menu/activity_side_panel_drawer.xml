<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        tools:showIn="navigation_view">

    <group
            android:id="@+id/consultationGroup"
            android:checkableBehavior="single">
            <item
                    android:id="@+id/nav_home"
                    android:icon="@drawable/ic_dashboard_black_24dp"
                    android:title="@string/menu_home" />
            <item
                    android:id="@+id/nav_search"
                    android:icon="@drawable/ic_school_black_24dp"
                    android:title="@string/menu_search" />
            <item
                    android:id="@+id/nav_course_search"
                    android:icon="@drawable/ic_class_black_24dp"
                    android:title="@string/menu_class_search" />
            <item
                    android:id="@+id/nav_settings"
                    android:icon="@drawable/ic_settings"
                    android:title="@string/menu_settings" />
    </group>

    <group android:id="@+id/editionGroup">
        <item android:title="@string/edition">
            <menu>
                <item
                        android:id="@+id/nav_register"
                        android:icon="@drawable/ic_group_add_black_24dp"
                        android:title="@string/menu_register" />
            </menu>
        </item>
    </group>

</menu>
